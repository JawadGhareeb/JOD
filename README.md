# JOD Expo App

JOD is an Expo Router app for Arabic-first mobile experiences.

## Development

```bash
npm install
npx expo start
```

## Notes

- The app uses file-based routing under `app/`.
- The public web share URL defaults to `https://jod.app`.
